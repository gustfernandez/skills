# PR Conventions

> **Stub file.** Fill with project-specific PR rules. Referenced by `/reviewer`, `/pr-creator`, and `/orchestrator` skills.

## Severity mapping

<!-- Define rule → severity (must-fix / should-fix / nit) mapping for /reviewer. -->

## Notion card patterns

<!-- Priority-ordered regex / patterns for parsing Notion ticket IDs from plan Context or --notion flag. -->

## Label / scope mapping by file extension

<!-- Map file extensions or paths → labels and scopes for /pr-creator. -->
<!-- Example:
- `*.tsx`, `*.ts` → label: frontend, scope: web
- `*.py` → label: backend, scope: api
-->

## Commit message format

<!-- Conventional Commits config: allowed types, scope rules, breaking-change marker, body/footer rules. -->
<!-- Example: type(scope): subject  — types: feat, fix, chore, docs, refactor, test, perf, build, ci -->

## PR title format

<!-- Title template, prefix rules, ticket inclusion, length limits. -->

## Reviewer assignment

<!-- Auto-reviewer rules: Copilot, CODEOWNERS, team mentions, etc. -->

## Screenshot requirements

<!-- When screenshots required (frontend changes, UI diffs). Author-scoped — only enforced on own PRs. -->

## Front/back labels

<!-- Front-end / back-end label rules. Author-scoped. -->
